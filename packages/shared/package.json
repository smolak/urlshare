{
  "name": "@urlshare/shared",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint \"./**/*.ts*\"",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "nanoid": "^4.0.2"
  },
  "devDependencies": {
    "@urlshare/tests-setup": "*"
  }
}
