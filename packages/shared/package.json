{
  "name": "@urlshare/shared",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "coverage": "vitest run --coverage",
    "lint": "eslint \"./**/*.ts*\"",
    "test": "vitest run"
  },
  "dependencies": {
    "nanoid": "^4.0.2"
  }
}
